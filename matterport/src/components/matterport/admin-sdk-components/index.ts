import { MpSdk } from "bundle/sdk";
import { dimensionType, makeDimension } from "./DimensionComponent";

export const initComponents = async (sdk: MpSdk) => {
  // await Promise.all([sdk.Scene.register(dimensionType, makeDimension)]);
};
